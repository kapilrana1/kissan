rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the Cloud Function (Admin SDK), which bypasses
    // these rules. Direct client access is denied.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
